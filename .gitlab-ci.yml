image: continuumio/miniconda3

stages:
  - deps
  - test
  - deploy

variables:
  DEPLOY_DOCS: "$CI_COMMIT_BRANC"

conda_env:
  stage: deps
  script:
    - conda env update -f gldeployenv.yaml
    - source activate nestor-dev
    - poetry install

fonts:
  stage: deps
  script:
    - apt-get update
    - apt-get install-y fontconfig fonts-roboto
  only:
    - dev
    - docs
    - master

tests:
  stage: test
  needs: ["conda_env"]
  script:
    - poetry run pytest

pages:
  stage: deploy
  needs: ["conda_env", "fonts"]
  script:
<<<<<<< HEAD
=======
    - apt-get update && apt-get install -y fontconfig fonts-roboto
>>>>>>> 7235d5aeb9dbbbd44ba714cbd5ebbbce450994e7
    - MKDOCS_SITE_URL = 'https://kea.ipages.nist.gov/nestor-suite/nestor/'
    - MKDOCS_REPO_URL = 'https://gitlab.nist.gov/gitlab/kea/nestor-suite/nestor/'
    - poetry run task deploy-docs
  artifacts:
    paths:
      - public
  only:
    - dev
    - docs
    - master
